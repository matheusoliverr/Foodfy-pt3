{% extends "layouts.njk" %}

{% block links %}
<a class="about-link" href="/about">Sobre</a>
<a class="recipe-link open" href="/recipes">Receitas</a>
{% endblock %}

{% block content %}

    <main>
        <section class="introduction-recipes extended">

            {% for recipe in recipes %}
            <div class="recipe-card">
                <img class="recipe-img" id="{{loop.index0}}" src="{{recipe.image}}" alt="{{recipe.title}}">
                <p class="recipe-name">{{recipe.title}}</p>
                <p class="creator-name">Por {{recipe.author}}</p>
            </div>
            {% endfor %}

             {% for recipe in recipes %}
            <div class="recipe-card">
                <img class="recipe-img" id="{{loop.index0}}" src="{{recipe.image}}" alt="{{recipe.title}}">
                <p class="recipe-name">{{recipe.title}}</p>
                <p class="creator-name">Por {{recipe.author}}</p>
            </div>
            {% endfor %}
            
        </section>
    </main>

     

    {% endblock %}
